"""bair pipelines — concrete commands wired against Bernard's ecosystem.

Each module in this package declares one :func:`xair.command_registry.command`
handler. Importing this package registers every command with xair's runtime.
"""
