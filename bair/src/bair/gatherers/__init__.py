"""bair gatherers — read context from Bernard's data sources.

Generic GitHub gatherers (PR, diff, issue) live in xair. This package holds
Bernard-specific gatherers — e.g. a fi-core eval-baseline reader for
`/ai-eval`, a benchmarks/perf_baseline.json delta reader, etc."""
