"""bair prompt builders — Bernard-tuned system + user messages.

xair ships generic prompt builders (e.g. review, remedy). bair overrides
where the tone/context needs to match Bernard's voice or his repos'
conventions (no emojis in code, conventional commits, etc.)."""
